# Published Content

- `portfolio-content.json` is the published content file used by the frontend.
- Edit content locally in `/admin`.
- After review, export JSON from the admin page.
- Replace `public/content/portfolio-content.json` with the exported file.
- Commit both `public/content/portfolio-content.json` and any new images under `public/gallery`.
